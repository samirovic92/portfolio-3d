# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main
env:
  EMAIL_JS_SERVICE_ID: ${{ secrets.REACT_APP_SECRET_KEY }}
  EMAIL_JS_TEMPLATE_ID: ${{ secrets.EMAIL_JS_TEMPLATE_ID }}
  EMAIL_JS_PUBLIC_KEY: ${{ secrets.EMAIL_JS_PUBLIC_KEY }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - name: Print Firebase Token (Not Recommended)
        run: echo "EMAIL_JS_PUBLIC_KEY= $EMAIL_JS_PUBLIC_KEY"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PORTFOLIO_3D_742CA }}
          channelId: live
          projectId: portfolio-3d-742ca
